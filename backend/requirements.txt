# Runtime dependencies for the FastAPI backend
# Install inside the backend venv:
#   cd backend
#   .\venv\Scripts\Activate.ps1
#   python -m pip install -r requirements.txt

fastapi>=0.112,<1.0
uvicorn[standard]>=0.30,<1.0
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
psycopg2-binary>=2.9,<3.0
requests>=2.32,<3.0

# Crypto / JOSE for H.1 manifest signing & JWKS
python-jose[cryptography]>=3.3,<4.0
cryptography>=42,<45

# Optional: load .env automatically on startup (not wired yet)
python-dotenv>=1.0,<2.0

# HTTP client for OPA integration (J.1)
httpx>=0.27,<1.0

# Payment providers (K.1)
stripe>=7.0,<8.0

# Async task processing (Phase 2 enhancement)
celery>=5.3,<6.0
redis>=5.0,<6.0

# Scheduling and notifications (Phase 2 enhancement)
apscheduler>=3.10,<4.0
apprise>=1.7,<2.0



# Performance testing (M.1)
locust>=2.27,<3.0

# Docker SDK for sandbox orchestration (N.1)
docker>=7.0,<8.0

# Core Intelligence Dependencies (Phase 2 Strategic Pivot)
# Autonomous Learning and Prompt Optimization
dspy-ai>=2.4,<3.0
mlflow>=2.10,<3.0
deepeval>=0.21,<1.0
scikit-learn>=1.4,<2.0

# Agent Builder and Natural Language Processing
langchain>=0.1,<1.0
spacy>=3.7,<4.0
jinja2>=3.1,<4.0

# Testing dependencies
pytest-asyncio>=0.21,<1.0
